[package]
name = "single-crate"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
anyhow = "1.0.86"
json = { package = "serde_json", version = "1.0" }
regex = "1"

[dev-dependencies]
tempfile = "3"

[build-dependencies]
cc = "1.0"
