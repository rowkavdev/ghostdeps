{
  "name": "fixture-refs-concurrently-args",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "bench": "concurrently -k -s first \"tsc -w\" \"autocannon -c 100 -d 30 localhost:3000/\""
  },
  "devDependencies": {
    "autocannon": "^8.0.0",
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0"
  }
}
