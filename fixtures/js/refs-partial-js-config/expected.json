{
  "description": "eslint.config.mjs imports a local module (eslint.shared.js) whose strings are not read as config, so reference analysis is incomplete (#149). eslint-plugin-foo has no visible usage but must not be reported unused with high confidence; manual review is the correct outcome.",
  "findings": [],
  "mustNotFind": [{ "kind": "unused", "dependency": "eslint-plugin-foo", "minConfidence": "high" }]
}
