{
  "name": "fixture-package-json-without-js",
  "private": true,
  "version": "0.0.0",
  "description": "Tooling-only manifest: prettier config lives here, but the project has no JS/TS source.",
  "devDependencies": {
    "prettier": "^3.4.0"
  }
}
