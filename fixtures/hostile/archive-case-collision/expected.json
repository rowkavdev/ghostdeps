{
  "description": "Two entries differing only by case ('A.txt' vs 'a.txt'). On case-insensitive filesystems the second overwrites the first; validators must be at least as strict as the most lenient consumer FS.",
  "archive": "archive.tar.gz",
  "expect": {
    "result": "reject",
    "code": "DUPLICATE_PATH"
  }
}
