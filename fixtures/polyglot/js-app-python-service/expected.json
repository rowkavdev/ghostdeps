{
  "scenario": "A JS app at the root with a Python service nested under it, analysed with the real JS adapter plus the test-only stub Python adapter. Proves multi-ecosystem mechanics only; it does not claim Python support.",
  "projectTree": [
    { "id": "javascript-typescript:." },
    { "id": "python:services/api", "parent": "javascript-typescript:." }
  ],
  "graphEcosystems": ["javascript-typescript", "python"],
  "findings": [
    { "rule": "cross-ecosystem-capability-overlap", "dependency": "axios", "kind": "info" },
    { "rule": "cross-ecosystem-capability-overlap", "dependency": "requests", "kind": "info" }
  ],
  "mustNotFind": [{ "rule": "unused" }]
}
