[project]
name = "fastapp"
version = "0.1.0"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
fast = ["orjson>=3.10"]
yaml = ["PyYAML>=6"]
docs = ["mkdocs>=1.6"]
all = ["orjson>=3.10", "PyYAML>=6"]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"
