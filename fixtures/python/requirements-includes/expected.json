{
  "description": "requirements.txt that includes requirements/base.txt, plus requirements/dev.txt with a -r include and a -c constraints file (#44). Includes resolve relative to the including file; constraint pins are not declarations.",
  "detection": { "minConfidence": 0.7, "projects": ["."], "packageManagers": { ".": ["pip"] } },
  "dependencies": [
    { "name": "flask", "kind": "runtime", "constraint": "==3.0.3", "declaredIn": "requirements/base.txt" },
    { "name": "gunicorn", "kind": "runtime", "constraint": ">=22", "declaredIn": "requirements/base.txt" },
    { "name": "pytest", "kind": "dev", "constraint": ">=8", "declaredIn": "requirements/dev.txt" }
  ],
  "mustNotFind": [{ "kind": "unused", "dependency": "werkzeug" }],
  "findings": []
}
