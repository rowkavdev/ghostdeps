[project]
name = "shopapp"
version = "0.1.0"
dependencies = [
  "Pillow>=10",
  "PyYAML>=6",
  "scikit-learn>=1.5",
  "requests>=2.32",
  "weird-tool==2.1",
  "psycopg2-binary>=2.9",
]

[build-system]
requires = ["hatchling>=1.24"]
build-backend = "hatchling.build"
