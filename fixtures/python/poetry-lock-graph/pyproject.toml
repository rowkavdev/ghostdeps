[tool.poetry]
name = "svc"
version = "0.1.0"
description = "fixture"
authors = ["Fixture <fixture@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
requests = "^2.32"

[tool.poetry.group.test.dependencies]
pytest = "^8"
